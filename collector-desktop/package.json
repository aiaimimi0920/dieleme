{
  "name": "fapaifang-collector-desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 1420",
    "build": "vite build",
    "test": "node --test src/*.test.ts src/*.test.mjs",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint --deny-warnings src tests playwright.config.ts",
    "test:smoke": "npm run build && playwright test",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.9.0"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "@tauri-apps/cli": "^2.9.0",
    "@types/node": "26.6.2",
    "oxlint": "1.85.0",
    "typescript": "7.0.2",
    "vite": "^8.0.13"
  }
}
